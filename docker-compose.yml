services:
  postgres:
    container_name: postgres_db
    image: postgres
    restart: unless-stopped
    volumes:
       - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    env_file:
      - .env

volumes:
    pgdata: